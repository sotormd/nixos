# lib

Helper functions for rest of the flake.

```console
lib
├── default.nix      # entrypoint
├── conditionals.nix # lib.choose and lib.optional
└── impermanence.nix # lib.persistMount and lib.persistDirs
```
