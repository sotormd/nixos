name: Evaluate SD Image

on:
  push:
  pull_request:

jobs:
  nix-checks:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Install Nix
        uses: cachix/install-nix-action@v25

      - name: SD image evaluation
        run: nix eval .#nixosConfigurations.image-sd.config.system.build.toplevel

